# Runtime dependencies only. Everything listed here is imported somewhere under
# src/ or tests/; anything that was carried "just in case" has been removed.

# Embeddings run on ONNX Runtime, not PyTorch.
#
# torch, transformers and sentence-transformers together pulled in the whole CUDA
# wheel stack and cost roughly 250 MB of resident memory just to import, which is
# what pushed the Render deploy over its 512 MB limit. The embedding model is
# all-MiniLM-L6-v2 in both cases; ChromaDB ships an ONNX build of it that needs
# onnxruntime instead.
onnxruntime>=1.17.0

flask>=2.0.0
flask-cors>=4.0.0
google-genai>=1.0.0
python-dotenv>=1.0.0

# Retrieval
chromadb>=0.4.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-classic>=0.1.0
rank_bm25>=0.2.2

# Risk classifier and evaluation metrics
scikit-learn>=1.3.0

# Corpus ingestion (data ingestion, run manually)
pypdf>=4.0.0
python-docx>=1.1.0
langchain-text-splitters>=0.2.0

# Tests
pytest>=7.0.0
