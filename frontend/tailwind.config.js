// Tailwind CSS v4 is configured from CSS (see src/index.css).
// This file only exists so editor tooling knows where to scan for class names.
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
}
