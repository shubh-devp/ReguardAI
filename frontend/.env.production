# Backend the deployed frontend talks to.
#
# Vite inlines this value at build time. The full path matters: the API lives at
# /api/audit, and posting to the bare site root returns 405 instead. Setting
# VITE_API_URL in the host's dashboard overrides this file.
VITE_API_URL=https://reguardai.onrender.com/api/audit
